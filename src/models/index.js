/**
 * Collect all the models in a single file so that it could be easy to handle new Model effectively, 
 * since all the Model can be added or removed in the file. 
 */
module.exports.Pitch = require("./pitch.model").Pitch;