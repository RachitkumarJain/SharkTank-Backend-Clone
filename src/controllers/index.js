/**
 * Collect all the Controllers in a single file so that it could be easy to handle new Controller effectively, 
 * since all the Controller can be added or removed in the file. 
 */
module.exports.pitchController = require("./pitch.controller");